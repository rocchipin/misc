@Configuration
public class ProxyRouter {

    @Bean
    public RouterFunction<ServerResponse> routes(ProxyHandler handler) {
        return RouterFunctions
                .route(RequestPredicates.GET("/proxy"), handler::handleRequest);
    }
}
