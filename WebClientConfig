@Configuration
public class WebClientConfig {

    @Value("${app.backend1.url}")
    private String backend1Url;

    @Bean
    public WebClient backend1Client() throws Exception {
        SslContext sslContext = SslContextBuilder.forClient()
                .keyManager(new File("keystore.p12"), "keystorePassword")
                .trustManager(InsecureTrustManagerFactory.INSTANCE) // ou un truststore configuré
                .build();

        HttpClient httpClient = HttpClient.create().secure(t -> t.sslContext(sslContext));

        return WebClient.builder()
                .clientConnector(new ReactorClientHttpConnector(httpClient))
                .baseUrl(backend1Url)
                .build();
    }
}
